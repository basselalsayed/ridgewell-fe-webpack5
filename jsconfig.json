{
  "compilerOptions": {
    // This must be specified if "paths" is set
    "baseUrl": ".",
    // Relative to "baseUrl"
    "paths": {
     "Components/*": ["./src/components/*"],
     "Actions/*": ["./src/store/actions/*"],
     "Helpers/*": ["./src/helpers/*"]
    }
  }
}