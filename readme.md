## Done:
* Moved from CRA to custom webpack config
* Using react-refresh
* Extracted custom autoEffect hooks
* loadable-components
* Redux offline queue middleware

## To do:
* Finish SSR
  - Webpack node setup
  - Serve correctly via express and fix routng issue
  - learn webpack-dev-server and webpack-dev-middleware

* Code splitting:
  - Use UniversalComponent throughout the codebase

* PWA progression:
  - Service worker tutorial
  - Workbox after?
  - redux
    * immer
    * redux-persist
    * localForage
